# Alset Tutor Academia

- **Alias:** `tutor.app.ans`
- **URL:** `/w/tutor.app.ans`
- **Rol:** onboarding guiado del equipo (Mind, agentes, apps, HTTP, CRUD, nichos) sin exponer código fuente propietario.

Niveles 0–9 en la UI. Chat de práctica usa `POST /api/mind/tick` con sesión `tutor-academia-*`.
